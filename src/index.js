import React from 'react';
import ReactDOM from 'react-dom';

import ListaContactosApp from "./container/ListaContactosApp";

ReactDOM.render(
  <React.StrictMode>
    <ListaContactosApp />
  </React.StrictMode>,
  document.getElementById('root')
);
